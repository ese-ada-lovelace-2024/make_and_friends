#Set the lowest version of cmake that can be used with this file
cmake_minimum_required(VERSION 3.10)

# set the project name (and the name f)
project(HelloWorld)

# add the executable
add_executable(hello hello_world.cpp)