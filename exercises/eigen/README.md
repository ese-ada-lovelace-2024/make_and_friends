This example uses the external C++ header library Eigen.

Try to write a Makefile or CMakeList.txt file which is set up to build an executable test.

On the docker version, the files are installed under the directory /usr/include/eigen3.